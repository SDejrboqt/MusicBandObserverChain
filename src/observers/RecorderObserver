package observers;

import observer.Observer;
import observer.Event;

public class RecorderObserver implements Observer {
    @Override
    public void update(Event event) {
        if (event.getType().equals("NOTE_PLAYED")) {
            System.out.println("[Recorder] 🎥 Started recording audio/video");
        }
    }
}